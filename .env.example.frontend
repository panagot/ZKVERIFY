# Optional: use for frontend when running in live zkVerify mode.
# Copy to .env.local in project root (or set in shell when running npm run dev).
# When set, "Prove human" will call the backend to submit real proofs to zkVerify Volta.

VITE_API_URL=http://localhost:3001
